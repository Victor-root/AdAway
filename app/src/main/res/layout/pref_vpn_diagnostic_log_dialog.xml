<?xml version="1.0" encoding="utf-8"?>
<!--
    Custom content view for the diagnostic log dialog (see PrefsVpnFragment#showDiagnosticLog).

    Using setMessage() with a very long string made the MaterialAlertDialog's own button bar
    grow past the screen height on a long log, forcing the user to scroll through the buttons
    themselves to find "Share"/"Copy"/"OK". Using a custom view instead means only THIS ScrollView
    (fixed height) scrolls; the dialog's button bar stays outside it and is always visible.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="320dp"
    android:paddingStart="24dp"
    android:paddingTop="8dp"
    android:paddingEnd="24dp"
    android:paddingBottom="8dp"
    android:scrollbars="vertical">

    <TextView
        android:id="@+id/diagnosticLogText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textAppearance="@style/TextAppearance.AppCompat.Body2"
        android:textIsSelectable="true" />

</ScrollView>
