<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AdAway" parent="Base.AdAway">
        <item name="colorPrimaryDark">?colorSurface</item>
    </style>

    <style name="Theme.AdAway.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryVariant</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorSecondary">@color/primary</item>
        <item name="colorSecondaryVariant">@color/primaryVariant</item>
        <item name="colorOnSecondary">@color/onPrimary</item>

        <item name="android:statusBarColor">?colorPrimary</item>
        <item name="android:navigationBarColor">?attr/colorSurface</item>
        <item name="android:windowLightNavigationBar">@bool/navigationBarIsLight</item>
    </style>

    <!--
        Theme.AdAway.Prefs has no night variant any more: its navigation bar now comes from
        Base.AdAway, whose declaration already resolves per mode, so redeclaring it here
        would only risk the two drifting apart again.
    -->
</resources>
